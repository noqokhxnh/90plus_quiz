package com.noqok.a90plus_quiz.ui.theme

import androidx.compose.ui.graphics.Color

val Dark900 = Color(0xFF0B1220) // Nền app
val Dark900Tint = Color(0xFF0F172A) // Nền banner cập nhật
val Dark800 = Color(0xFF111827) // Bề mặt/Surface card
val Dark700 = Color(0xFF1F2937) // Viền mềm (opacity 60% sẽ được áp dụng khi sử dụng)

val PrimaryBlue = Color(0xFF3B82F6) // Cho "Quiz", nút, highlight
val PrimaryBlueDark = Color(0xFF1976D2) // Darker shade for gradients
val AccentYellow = Color(0xFFF6C12A) // Cho "90+", badge NEW
val AccentYellowDark = Color(0xFFFFB300) // Darker shade for gradients

val GreenStatus = Color(0xFF22C55E) // Trạng thái thắng
val GreenStatusDark = Color(0xFF16A34A) // Darker shade for gradients
val RedStatus = Color(0xFFEF4444) // Trạng thái thua
val RedStatusDark = Color(0xFFDC2626) // Darker shade for gradients

val TextHighEmphasis = Color(0xFFE5E7EB) // Chữ chính
val TextMediumEmphasis = Color(0xFF9CA3AF) // Chữ phụ

// Additional colors for enhanced UI
val SuccessGreen = Color(0xFF4CAF50) // Success states
val SuccessGreenDark = Color(0xFF2E7D32) // Darker success
val WarningOrange = Color(0xFFFF9800) // Warning states
val WarningOrangeDark = Color(0xFFE65100) // Darker warning
